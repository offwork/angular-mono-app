import { NestedMenuPositionDirective } from './nested-menu-position.directive';

describe('NestedMenuPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new NestedMenuPositionDirective();
    expect(directive).toBeTruthy();
  });
});
