import { ComponentTranslationModel } from './component.model';

describe('Component', () => {
  it('should create an instance', () => {
    expect(new ComponentTranslationModel()).toBeTruthy();
  });
});
