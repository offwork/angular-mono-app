import { UrlRegexpInterceptorMatcher } from './url-regexp-interceptor-matcher';

describe('UrlRegexpInterceptorMatcher', () => {
  it('should create an instance', () => {
    expect(new UrlRegexpInterceptorMatcher()).toBeTruthy();
  });
});
