import { SidenavLayoutFooterDirective } from './sidenav-layout-footer.directive';

describe('SidenavLayoutFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavLayoutFooterDirective();
    expect(directive).toBeTruthy();
  });
});
